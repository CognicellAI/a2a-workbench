# A2A Workbench Architecture

The normative target architecture is maintained in the
[architecture documentation](./docs/architecture/README.md).

That documentation defines the system boundaries, C4 model, public client API,
protocol invariants, security controls, and conformance strategy. It intentionally
does not describe the repository's historical or transitional implementation.
